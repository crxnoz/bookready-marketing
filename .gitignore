.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
node_modules/

# Local-only dev tooling (preview server + launch config) — must never deploy
.claude/

# Scratch dir for one-off generators / experiments — never deploy
.scratch/

# Keyword-research intermediates (datasets, audits, API cache) — regenerable, never deploy.
# The reviewable artifact (content/queue.proposed.json) lives outside this dir and IS tracked.
content/research/

# Stock-image experiment: staging candidates + the compare page — local only, never deploy
images/blog/_stock/
_stock-compare.html

# Secrets (DataForSEO etc.) — never commit. Use .env.example as the template.
.env
.env.local
